Portaudio examples 
Dueltest ;example of bridging streams via a ring buffer from two threads (note you need to build the ring buffer first) 
fftest   ;uses a single FX callback on the input output and runs an FFT iFFT and plays it 

